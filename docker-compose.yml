version: "3.8"

services:

  backend:
    build: ./backend
    container_name: blog_backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: blog_frontend
    ports:
      - "5173:80"
    depends_on:
      - backend